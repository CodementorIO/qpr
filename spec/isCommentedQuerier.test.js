import { querier } from '../src2/isCommentedQuerier'

describe('isCommentedQuerier({ username, token, number })', ()=> {
  it('sends request to github to fetch reveiws and requested_reviewers', ()=> {
    expect(true).toEqual(false)
  })
})
